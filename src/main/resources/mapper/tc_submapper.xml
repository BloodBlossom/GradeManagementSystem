<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.program.dao.tc_subDao">
    <select id="selectById" parameterType="Integer" resultType="Subject">
        select * from `su_info` where tc_ID=#{tc_id}
    </select>
</mapper>